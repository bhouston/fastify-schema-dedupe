{
  "name": "basic-example",
  "type": "module",
  "scripts": {
    "build": "tsgo",
    "dev": "node --watch --watch-preserve-output src/server.ts",
    "start": "node src/server.ts",
    "tsgo": "tsgo --noEmit"
  },
  "dependencies": {
    "ajv": "^8.0.0",
    "fastify": "^5.0.0",
    "fastify-schema-dedupe": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24"
  }
}
